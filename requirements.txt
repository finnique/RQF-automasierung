# Pip deps for the Linux container (web/Dockerfile). Local Windows dev
# still uses the conda env in environment.yml for WeasyPrint's native
# libs; this file is for the Docker image, which installs those libs via
# apt instead (see Dockerfile).
fastapi>=0.110
uvicorn[standard]>=0.29
jinja2==3.1.6
pydantic==2.13.5
weasyprint==69.0
openai==3.13.0
python-dotenv==1.2.3
